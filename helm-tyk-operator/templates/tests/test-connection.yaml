apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-tyk-operator.fullname" . }}-test-connection"
  labels:
    {{- include "helm-tyk-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-tyk-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
