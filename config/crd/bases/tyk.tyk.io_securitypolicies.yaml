
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: securitypolicies.tyk.tyk.io
spec:
  group: tyk.tyk.io
  names:
    kind: SecurityPolicy
    listKind: SecurityPolicyList
    plural: securitypolicies
    shortNames:
    - tykpolicies
    singular: securitypolicy
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SecurityPolicy is the Schema for the securitypolicies API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SecurityPolicySpec defines the desired state of SecurityPolicy
            properties:
              _id:
                description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file'
                type: string
              access_rights:
                additionalProperties:
                  description: from tyk/session.go AccessDefinition defines which
                    versions of an API a key has access to
                  properties:
                    allowance_scope:
                      type: string
                    allowed_urls:
                      items:
                        description: from tyk/session.go AccessSpecs define what URLS
                          a user has access to an what methods are enabled
                        properties:
                          methods:
                            items:
                              type: string
                            type: array
                          url:
                            type: string
                        required:
                        - methods
                        - url
                        type: object
                      type: array
                    api_id:
                      type: string
                    api_name:
                      type: string
                    limit:
                      description: RestrictedTypes []graphql.Type `json:"restricted_types"`
                      properties:
                        max_query_depth:
                          type: integer
                        per:
                          format: int64
                          type: integer
                        quota_max:
                          format: int64
                          type: integer
                        quota_remaining:
                          format: int64
                          type: integer
                        quota_renewal_rate:
                          format: int64
                          type: integer
                        quota_renews:
                          format: int64
                          type: integer
                        rate:
                          format: int64
                          type: integer
                        throttle_interval:
                          format: int64
                          type: integer
                        throttle_retry_limit:
                          type: integer
                      required:
                      - max_query_depth
                      - per
                      - quota_max
                      - quota_remaining
                      - quota_renewal_rate
                      - quota_renews
                      - rate
                      - throttle_interval
                      - throttle_retry_limit
                      type: object
                    versions:
                      items:
                        type: string
                      type: array
                  required:
                  - allowance_scope
                  - allowed_urls
                  - api_id
                  - api_name
                  - versions
                  type: object
                type: object
              access_rights_array:
                items:
                  description: from tyk/session.go AccessDefinition defines which
                    versions of an API a key has access to
                  properties:
                    allowance_scope:
                      type: string
                    allowed_urls:
                      items:
                        description: from tyk/session.go AccessSpecs define what URLS
                          a user has access to an what methods are enabled
                        properties:
                          methods:
                            items:
                              type: string
                            type: array
                          url:
                            type: string
                        required:
                        - methods
                        - url
                        type: object
                      type: array
                    api_id:
                      type: string
                    api_name:
                      type: string
                    limit:
                      description: RestrictedTypes []graphql.Type `json:"restricted_types"`
                      properties:
                        max_query_depth:
                          type: integer
                        per:
                          format: int64
                          type: integer
                        quota_max:
                          format: int64
                          type: integer
                        quota_remaining:
                          format: int64
                          type: integer
                        quota_renewal_rate:
                          format: int64
                          type: integer
                        quota_renews:
                          format: int64
                          type: integer
                        rate:
                          format: int64
                          type: integer
                        throttle_interval:
                          format: int64
                          type: integer
                        throttle_retry_limit:
                          type: integer
                      required:
                      - max_query_depth
                      - per
                      - quota_max
                      - quota_remaining
                      - quota_renewal_rate
                      - quota_renews
                      - rate
                      - throttle_interval
                      - throttle_retry_limit
                      type: object
                    versions:
                      items:
                        type: string
                      type: array
                  required:
                  - allowance_scope
                  - allowed_urls
                  - api_id
                  - api_name
                  - versions
                  type: object
                type: array
              active:
                type: boolean
              enable_http_signature_validation:
                type: boolean
              hmac_enabled:
                type: boolean
              id:
                type: string
              is_inactive:
                type: boolean
              key_expires_in:
                description: KeyExpiresIn is the number of seconds till key expiry.
                  For 1 hour is 3600. Default never expire or 0
                format: int64
                type: integer
              max_query_depth:
                type: integer
              name:
                type: string
              org_id:
                type: string
              partitions:
                properties:
                  acl:
                    type: boolean
                  complexity:
                    type: boolean
                  per_api:
                    type: boolean
                  quota:
                    type: boolean
                  rate_limit:
                    type: boolean
                required:
                - acl
                - complexity
                - per_api
                - quota
                - rate_limit
                type: object
              per:
                format: int64
                type: integer
              quota_max:
                format: int64
                type: integer
              quota_renewal_rate:
                format: int64
                type: integer
              rate:
                format: int64
                type: integer
              state:
                description: State can be active, draft or deny
                type: string
              tags:
                items:
                  type: string
                type: array
              throttle_interval:
                format: int64
                type: integer
              throttle_retry_limit:
                type: integer
            required:
            - access_rights_array
            - active
            - is_inactive
            - key_expires_in
            - max_query_depth
            - name
            - per
            - quota_max
            - quota_renewal_rate
            - rate
            - state
            - throttle_interval
            - throttle_retry_limit
            type: object
          status:
            description: SecurityPolicyStatus defines the observed state of SecurityPolicy
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
