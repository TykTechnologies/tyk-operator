apiVersion: tyk.tyk.io/v1
kind: ApiDefinition
metadata:
  name: httpbin
spec:
  org_id: acme
  proxy:
    listen_path: /foo
    strip_listen_path: true
    target_url: http://httpbin.org
  protocol: http

#  proxy:
#    transport:
#      ssl_ciphers: []
