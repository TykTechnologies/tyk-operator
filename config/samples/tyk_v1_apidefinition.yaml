apiVersion: tyk.tyk.io/v1
kind: ApiDefinition
metadata:
  name: httpbin
spec:
  api_id: httpbin
  org_id: acme
  listen_path: /httpbin
  proxy:
    target_url: http://httpbin.org
  protocol: http

#  proxy:
#    transport:
#      ssl_ciphers: []
